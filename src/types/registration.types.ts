type registrationProps = { userName: string; email: string; password: string, comfirmPassword: string };

export type loginProps = { identity: string, password: string };

export default registrationProps;
